# Examples: Knowledge OS — Real Usage Scenarios

Этот файл показывает **как системой реально пользоваться**.
Без теории. Только воспроизводимые шаги.

---

## Example 1 — Найти старую мысль и извлечь вывод

**Ситуация**  
Ты уверен, что уже обсуждал «executable protocols vs Jira»,  
но не помнишь где и не хочешь думать заново.

### Шаги

1. **Поиск в архиве:**
   ```bash
   SEARCH archive export_path=/Users/me/Downloads/conversations.json query="executable protocols"
   ```
   
   → видишь 3 результата  
   → выбираешь нужный `conversation_id`

2. **Извлечение фрагмента:**
   ```bash
   python3 tools/extract_snippet.py --id 674f7aa8-eb00-8000-9fff-27fd1e9ad6d6
   ```
   
   Читаешь фрагмент → понимаешь, что там есть чёткий вывод.

3. **Предложение извлечения:**
   ```
   SUGGEST extract
   ```
   
   → агент предлагает:
   - conclusion: "Jira workflows are human protocols, not AI protocols"

4. **Подтверждение:**
   ```
   ИЗВЛЕЧЬ вывод
   ```

✅ Создан block  
❌ Ничего не придумано заново

---

## Example 2 — Из блока сделать статью

**Ситуация**  
Есть 3 blocks по теме `jira-decline`.

### Шаги

1. **Предложение сборки:**
   ```
   SUGGEST assembly
   ```
   
   → агент предлагает:
   - article (medium)
   - blog post (short)

2. **Фиксация намерения:**
   ```
   MARK as article candidate
   ```

3. **Когда готов публиковать:**
   ```
   ASSEMBLE article
   from: candidate
   ```

✅ Текст собран  
✅ Есть Sources  
❌ Нет новых идей “из воздуха”

---

## Example 3 — Чеклист → книга

**Ситуация**  
Есть сильный checklist, который явно тянет на книгу.

### Шаги

1. **Создание кандидата:**
   ```
   MARK as book_section candidate
   ```

2. **Через неделю — проверка готовности:**
   ```
   SUGGEST assembly
   ```
   
   → агент говорит:
   - "Good as Appendix"
   - "Needs 1 framework for core chapter"

3. **Добавление недостающего блока:**
   ```
   ИЗВЛЕЧЬ фреймворк
   ```

4. **Сборка книги:**
   ```
   ASSEMBLE book
   from: candidate
   ```

✅ Чеклист встроен в книгу  
✅ Архитектура выдержана

---

## Example 4 — Когда НЕ надо извлекать

**Ситуация**  
Ты рассуждаешь, сомневаешься, перебираешь формулировки.

### Правильное поведение

❌ **НЕ:**
```
ИЗВЛЕЧЬ что-нибудь
```

✅ **ДА:**
```
SUGGEST extract
```

Если агент не предлагает ничего ценного —  
значит, мысль ещё не созрела.

---

## Example 5 — Предотвращение повтора мышления

**Ситуация**  
Хочешь написать письмо клиенту про AI-агентов.

### Шаги

1. **Поиск в архиве:**
   ```bash
   SEARCH archive query="ai agents email"
   ```
   
   → находишь старое обсуждение  
   → извлекаешь checklist

2. **Сборка письма:**
   ```
   ASSEMBLE email
   theme: ai-agents
   length: short
   ```

✅ Письмо готово  
❌ Нет повторного “придумывания”

---

## Example 6 — Система начала ломаться (и ты это поймал)

**Симптом**
- много draft-candidates
- мало output

### Проверка

Открываешь:
- `ANTI-PATTERNS.md`
- `CHECKLIST.md`

Находишь:
- AP-05 (вечные draft)
- AP-11 (система ради системы)

### Исправление
- депрецируешь лишние candidates
- собираешь один реальный output

---

## Example 7 — Минимальный ежедневный ритуал (5 минут)

В конце дня:

1. ```bash
   SEARCH archive
   ```
   (если было много разговоров)

2. ```
   ИЗВЛЕЧЬ вывод
   ```
   (1 максимум)

3. ```bash
   git commit -m "Extracted insight on <theme>"
   ```

📌 Этого достаточно, чтобы система жила годами.

---

## Итог

Если ты:
- ищешь → прежде чем думать
- извлекаешь → прежде чем писать
- собираешь → вместо генерации

👉 **Knowledge OS работает правильно.**

Если нет — смотри `ANTI-PATTERNS.md`.