# CHECKLIST.md
## Knowledge OS — Pre-Flight & Health Check

Этот чеклист используется:
- перед большими сессиями
- перед публикацией
- после изменений в агентах/командах
- когда «что-то перестало работать»

Цель: **убедиться, что система думает чисто и не портит знания**.

---

## 🧠 1. Командная дисциплина (CRITICAL)

- [ ] Все действия запускаются **только** командами из `COMMANDS.md`
- [ ] Разговорная речь **не вызывает** файловых изменений
- [ ] Русские команды используются **строго в канонической форме**
- [ ] Нет «угадывания намерений» агентами

❌ Если здесь есть сбой — остановись.  
Чини команды, не контент.

---

## 🔹 2. SUGGEST режим (SAFETY)

- [ ] `SUGGEST extract` **не создаёт** files
- [ ] `SUGGEST assembly` **не пишет** в `output/`
- [ ] После SUGGEST требуется явное подтверждение
- [ ] Нет “скрытых” side effects

📌 SUGGEST = мышление, не фиксация.

---

## 🧱 3. Blocks (Источник истины)

Проверь 3–5 случайных blocks:

- [ ] 1 файл = 1 идея
- [ ] Идея самодостаточна вне контекста чата
- [ ] Есть frontmatter
- [ ] Указан `type`
- [ ] Есть минимум 1 `theme`
- [ ] `reuse` заполнен
- [ ] Нет дат в имени файла

❌ Если блок слабый — **исправь или депрецируй**, не игнорируй.

---

## 🧭 4. Candidates (Намерение, не текст)

- [ ] Candidates не дублируют blocks
- [ ] Есть `status`
- [ ] Есть `source_blocks`
- [ ] Понятно, чем это может стать (глава / статья / пост)
- [ ] Нет «вечных draft без смысла»

📌 Candidates — это карта будущего, не архив сомнений.

---

## 🏗️ 5. Assembly (Сборка без фантазии)

- [ ] ASSEMBLE использует **только** blocks / candidates
- [ ] Соблюдается `knowledge/pipelines/pipeline.yaml`
- [ ] В output есть секция `Sources`
- [ ] Sources указывают на `knowledge/blocks/...`
- [ ] При нехватке blocks сборка **останавливается**

❌ Если ассемблер «додумывает» — система сломана.

---

## 🔍 6. ArchiveSearch (Память)

- [ ] SEARCH archive не пишет в `knowledge/`
- [ ] Индекс `index/chats.sqlite` существует и обновляем
- [ ] Архив используется **только** как источник поиска
- [ ] Экстракция из архива происходит **осознанно**

📌 Архив = память.  
📌 Blocks = решения.

---

## 🗂️ 7. Organizer (Гигиена)

- [ ] Нет дублирующих blocks с одинаковым смыслом
- [ ] Themes стабильны (не проекты, не даты)
- [ ] Tags нормализованы
- [ ] Deprecated blocks помечены явно
- [ ] Ничего не удаляется без причины

---

## 🔐 8. Безопасность и обратимость

- [ ] Репозиторий под git
- [ ] Коммиты маленькие и осмысленные
- [ ] Можно откатиться на любой шаг
- [ ] Нет “магических” изменений без коммита

---

## 🧩 9. Быстрый ментальный тест (FINAL)

Ответь «да» на все вопросы:

- [ ] Я знаю, **какие идеи у меня есть**
- [ ] Я вижу, **что из этого — кандидаты**
- [ ] Я понимаю, **что уже использовано**
- [ ] Я могу собрать контент **без генерации заново**

Если хотя бы одно «нет» —  
чинить **структуру**, не писать новый текст.

---

## 🟢 Золотое правило

> **Если это не block — этого не существует.**

Этот чеклист существует,
чтобы ты не нарушал это правило случайно.
